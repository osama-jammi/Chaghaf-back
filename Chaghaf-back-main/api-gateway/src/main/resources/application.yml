server:
  port: 8080

spring:
  application:
    name: api-gateway

  # ✅ Désactiver Redis tant qu'il n'est pas utilisé
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration

  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=0

        - id: subscription-service
          uri: lb://subscription-service
          predicates:
            - Path=/api/subscriptions/**,/api/packs/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

        - id: reservation-service
          uri: lb://reservation-service
          predicates:
            - Path=/api/reservations/**,/api/salles/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

        - id: boisson-service
          uri: lb://boisson-service
          predicates:
            - Path=/api/boissons/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

        - id: snack-service
          uri: lb://snack-service
          predicates:
            - Path=/api/snacks/**,/api/orders/**,/api/panier/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

        - id: social-service
          uri: lb://social-service
          predicates:
            - Path=/api/posts/**,/api/messages/**,/api/conversations/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications/**
          filters:
            - StripPrefix=0
            - JwtAuthFilter

eureka:
  client:
    # 🔴 CHANGEMENT CRITIQUE : utiliser le nom du conteneur Docker, pas localhost
    service-url:
      defaultZone: http://admin:chaghaf2025@discovery-service:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.uuid}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway
  endpoint:
    gateway:
      enabled: true
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:chaghaf-super-secret-key-2025-agadir-morocco-production}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.cloud.loadbalancer: DEBUG
    reactor.netty: INFO